# CyMind v2.0.0 使用指南

## 🎉 欢迎使用 CyMind 增强版！

CyMind v2.0.0 是一个功能强大的自动化渗透测试平台，现在具备了更强的核心架构和增强功能。

## 🚀 快速开始

### 1. 启动平台

```bash
# 方式1: 使用新的启动脚本（推荐）
python3 start_server.py

# 方式2: 使用传统方式
python3 run.py

# 方式3: 直接运行Flask应用
python3 app.py
```

### 2. 访问Web界面

启动后，在浏览器中访问：
- **主界面**: http://127.0.0.1:5000
- **API文档**: http://127.0.0.1:5000/api/targets

## 🔧 核心功能

### 目标管理
- ✅ 添加扫描目标（URL或IP地址）
- ✅ 批量导入CSV文件
- ✅ 目标分类和标签管理
- ✅ 目标验证和格式检查

### 扫描功能
- ✅ 端口扫描（基于Nmap）
- 🔄 子域名枚举（开发中）
- 🔄 漏洞扫描（开发中）
- 🔄 Web目录扫描（开发中）

### 报告系统
- ✅ HTML格式报告
- 🔄 PDF格式报告（开发中）
- 🔄 Markdown格式报告（开发中）

## 📊 新增功能（v2.0.0）

### 🧠 核心架构增强
- **模块化设计**: 清晰的接口定义和组件分离
- **配置管理**: 统一的YAML配置文件支持
- **智能日志**: 分级日志记录和文件轮转
- **错误处理**: 完善的异常处理和恢复机制

### 🧪 测试框架
- **属性测试**: 集成Hypothesis进行基于属性的测试
- **单元测试**: 完整的pytest测试套件
- **测试策略**: 智能数据生成和边界条件测试

### ⚙️ 配置系统
- **环境变量**: 支持通过环境变量配置
- **配置验证**: 自动验证配置的正确性
- **热重载**: 支持配置文件的动态加载

## 🌐 API 接口

### 目标管理 API

```bash
# 获取所有目标
curl http://127.0.0.1:5000/api/targets

# 添加新目标
curl -X POST http://127.0.0.1:5000/api/targets \
  -H "Content-Type: application/json" \
  -d '{"name": "测试目标", "url": "example.com", "type": "website"}'
```

### 扫描 API

```bash
# 启动端口扫描
curl -X POST http://127.0.0.1:5000/api/scan \
  -H "Content-Type: application/json" \
  -d '{"target": "scanme.nmap.org", "scan_type": "port_scan"}'

# 查看扫描状态
curl http://127.0.0.1:5000/api/scan/{scan_id}
```

### 历史记录 API

```bash
# 获取扫描历史
curl http://127.0.0.1:5000/api/history

# 获取具体扫描结果
curl http://127.0.0.1:5000/api/history/{scan_id}
```

## ⚙️ 配置文件

### cymind.yaml 配置示例

```yaml
# 基本配置
debug: true
host: "127.0.0.1"
port: 5000

# 数据库配置
database:
  url: "sqlite:///cymind.db"
  echo: false

# 扫描器配置
scanner:
  max_concurrent_scans: 5
  default_timeout: 300
  nmap_path: "nmap"

# 日志配置
logging:
  level: "INFO"
  log_dir: "logs"
  enable_console: true

# 安全配置
security:
  secret_key: "your-secret-key"
  session_timeout: 3600
```

## 🧪 运行测试

```bash
# 运行所有测试
python3 -m pytest tests/ -v

# 运行核心结构测试
python3 -m pytest tests/test_core_structure.py -v

# 运行演示脚本
python3 demo.py

# 运行启动测试
python3 test_startup.py
```

## 📁 项目结构

```
CyMind/
├── 🧠 core/                    # 核心模块
│   ├── interfaces.py          # 接口定义
│   ├── exceptions.py          # 异常类
│   ├── logging_config.py      # 日志配置
│   ├── config.py             # 配置管理
│   └── error_handler.py      # 错误处理
├── 🧪 tests/                   # 测试目录
│   ├── conftest.py           # pytest 配置
│   ├── test_core_structure.py
│   └── property_tests/       # 属性测试
├── 🔧 modules/                 # 功能模块
│   ├── target_manager.py
│   ├── scanner.py
│   └── reporter.py
├── 🎨 templates/               # 模板文件
├── 📊 static/                  # 静态文件
├── ⚙️ cymind.yaml             # 配置文件
├── 📋 requirements.txt        # 依赖管理
└── 🚀 start_server.py         # 启动脚本
```

## 🔧 故障排除

### 常见问题

1. **端口被占用**
   ```bash
   # 检查端口占用
   lsof -i :5000
   # 或修改配置文件中的端口
   ```

2. **依赖缺失**
   ```bash
   # 安装依赖
   pip3 install -r requirements.txt
   ```

3. **nmap未安装**
   ```bash
   # macOS
   brew install nmap
   
   # Ubuntu/Debian
   sudo apt-get install nmap
   
   # CentOS/RHEL
   sudo yum install nmap
   ```

4. **配置文件错误**
   ```bash
   # 使用示例配置
   cp cymind.yaml.example cymind.yaml
   # 然后编辑配置文件
   ```

### 日志查看

```bash
# 查看应用日志
tail -f logs/cymind.log

# 查看错误日志
tail -f logs/error.log

# 查看扫描日志
tail -f logs/scan.log
```

## 🚀 下一步开发

根据任务计划，接下来将实现：

1. **增强数据模型** - 扩展数据库架构
2. **完善目标管理** - 添加更多管理功能
3. **扩展扫描模块** - 集成更多扫描工具
4. **报告系统** - 多格式报告生成
5. **AI助手** - 智能分析和建议
6. **插件系统** - 第三方工具集成

## 📞 支持

如有问题或建议，请：
1. 查看日志文件获取详细错误信息
2. 检查配置文件是否正确
3. 确认所有依赖已正确安装
4. 参考本使用指南进行故障排除

---

**CyMind v2.0.0** - 让渗透测试更智能、更高效！ 🧠💻